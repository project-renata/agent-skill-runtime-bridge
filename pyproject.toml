[project]
name = "agent-skill-runtime-bridge"
version = "0.6.0"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.4.7,<4",
    "py-key-value-aio[redis]>=0.4.5",
]

[dependency-groups]
dev = [
    "workers-py",
]
